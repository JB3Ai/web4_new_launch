name: My MCP Setup
version: 0.0.1
schema: v1

mcpServers:
  - name: Filesystem MCP
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem"
      - "C:\\Apps in Dev Visual Code Folder"
    env: {}